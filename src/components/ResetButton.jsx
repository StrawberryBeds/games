// ResetButton.jsx
import React from "react";

function ResetButton({ onClick }) {
  return (
    <button onClick={onClick}>
      Play a new game !
    </button>
  );
}

export default ResetButton;


