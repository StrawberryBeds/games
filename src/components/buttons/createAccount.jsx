// src/components/buttons/createAccount.jsx
import React from 'react';

const createAccountButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>
      Create Your Account
    </button>
  );
};

export default createAccountButton;
